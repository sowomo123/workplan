# Example environment variables for local development
# Replace values with your own. WORKOS redirect should point to the callback route handled by AuthKit.

# The URL users will be redirected back to after sign in (must match your WorkOS configuration)
WORKOS_REDIRECT_URI=http://localhost:3000/callback

# Optional: where to redirect after logout
WORKOS_POST_LOGOUT_REDIRECT_URI=http://localhost:3000/

# Common fallbacks used by some libraries
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# WorkOS API key (keep private)
WORKOS_API_KEY=sk_live_XXXXXXXXXXXXXXXXXXXX
